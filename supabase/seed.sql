-- Employee Payroll Loan Portal — local dev seed.
-- Real seed data (employers, branches, demo accounts, sample applications,
-- contracts, etc.) is added in Phase 1 once the schema exists.
--
-- This file is consumed by `supabase db reset` during local development.

SELECT 'seed.sql is a placeholder; real data lands in Phase 1' AS notice;
